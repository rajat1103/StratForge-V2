# Database (PostgreSQL - use Supabase, Neon, or local)
DATABASE_URL="postgresql://user:password@localhost:5432/stratforge"

# AI (Groq API Key - console.groq.com)
GROQ_API_KEY="gsk_..."

# Auth secret (generate: openssl rand -hex 32)
JWT_SECRET="your-super-secret-jwt-key-change-in-production"

# App URL
NEXT_PUBLIC_APP_URL="http://localhost:3000"
